diff --git a/apps/web/lib/live.ts b/apps/web/lib/live.ts
new file mode 100644
index 0000000..95f1e1f
--- /dev/null
+++ b/apps/web/lib/live.ts
@@
+import { API_BASE } from "./api";
+
+export function subscribeToLive(onMessage: (ev: MessageEvent) => void): () => void {
+  const es = new EventSource(`${API_BASE}/live/subscribe`);
+  es.onmessage = onMessage;
+  return () => es.close();
+}

